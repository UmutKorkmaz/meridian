// This file is emitted by the source generator itself into consumer
// compilations. Keeping it in the generator project is a convention so
// the canonical text lives here and can be reviewed alongside the
// generator that injects it.
//
// The attribute is emitted with `internal` visibility so it does not
// become part of any consumer's public API. Each consumer assembly gets
// its own private copy — this is the same pattern System.Text.Json's
// JsonSerializableAttribute uses.
